<h2><%= @trip.name %></h2>

<% @trip.pictures.each do |picture| %>
  <%= image_tag picture.photo.url(:medium) %>
<% end %>

<%= link_to "Add Picture", new_trip_picture_path(@trip), :class => "btn btn-default" %>
<%= link_to "Home", root_path, :class => "btn btn-default" %>
