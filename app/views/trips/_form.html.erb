<%= form_for @picture, :url => trip_pictures_path(@trip), :html => { :multipart => true } do |f| %>
  <%= render('layouts/errors', :object => @picture) %>
  <%= f.file_field :photo %>
  <%= f.submit "Add Photo", :class => "btn btn-default" %>
<% end %>
