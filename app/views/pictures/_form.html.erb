<%= form_for [@trip, @picture], :html => { :multipart => true } do |f| %>
  <%= render('layouts/errors', :object => @picture) %>
  <%= f.file_field :photo %>
  <%= f.submit(:class => "btn btn-default") %>
<% end %>

