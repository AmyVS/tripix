<%= form_for [@trip, @picture], :url => trip_pictures_path, :html => { :multipart => true } do |f| %>
  <%= render('layouts/errors', :object => @picture) %>
  <%= f.file_field :photo %>
  <%= f.submit "Add Photo", :class => "btn btn-default" %>
<% end %>

